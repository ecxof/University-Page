# University App Data Dashboard

React + Vite + Tailwind + shadcn/ui dashboard for a university-facing web app.

## Getting started

```bash
npm install
npm run dev
```

Then open http://localhost:3000.

### Scripts

| Script | What it does |
| --- | --- |
| `npm run dev` | Vite dev server on port 3000 |
| `npm run build` | Production build to `build/` |
| `npm run preview` | Serve the production build locally |
| `npm run typecheck` | `tsc --noEmit` type check |

## Project structure

```
University_App_Dashboard/
├── index.html
├── package.json
├── tsconfig.json
├── vite.config.ts
├── docs/                          # design notes, attributions, guidelines
└── src/
    ├── main.tsx                   # entry
    ├── App.tsx                    # wires the router
    ├── index.css                  # pre-compiled Tailwind runtime
    ├── app/                       # router + root layout
    │   ├── routes.ts
    │   └── Root.tsx
    ├── pages/                     # one file per route
    │   ├── HomePage.tsx
    │   ├── AcademicsPage.tsx
    │   ├── AccountPage.tsx
    │   ├── AdmissionsPage.tsx
    │   ├── CampusLifePage.tsx
    │   └── NotificationsPage.tsx
    ├── features/                  # feature-scoped composites
    │   └── dashboard/             # analytics widgets used on HomePage
    │       ├── StatsCards.tsx
    │       ├── EnrollmentChart.tsx
    │       ├── DepartmentBreakdown.tsx
    │       ├── GradeDistribution.tsx
    │       ├── CourseTable.tsx
    │       └── RecentActivities.tsx
    ├── components/
    │   ├── layout/                # site chrome (Navigation, Footer, Hero)
    │   ├── common/                # cross-page building blocks
    │   │   ├── QuickLinks.tsx
    │   │   ├── ContactSection.tsx
    │   │   ├── SearchOverlay.tsx
    │   │   └── ImageWithFallback.tsx
    │   └── ui/                    # shadcn/ui primitives (do not edit directly)
    └── styles/
        └── globals.css
```

### Where to put new code

- **A new URL** → add a page in `src/pages/` and register it in `src/app/routes.ts`.
- **A widget used on one page** → put it next to that page or in
  `src/features/<feature>/`.
- **A widget reused across pages** → `src/components/common/`.
- **Site chrome** (nav, footer, hero) → `src/components/layout/`.
- **A shadcn primitive** → generate it into `src/components/ui/` — do not hand-edit.

## Notes on the shadcn `ui/` folder

The files in `src/components/ui/` were originally generated by Figma Make and use
version-suffixed import specifiers such as `@radix-ui/react-accordion@1.2.3`. Vite
resolves those to the plain package names via aliases in `vite.config.ts`. If you
regenerate the primitives with a stock shadcn CLI, drop that alias block.
